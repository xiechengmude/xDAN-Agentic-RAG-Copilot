# xDAN Rag Copilot API Service 真实接口情况

## 测试时间
2025-06-26 19:32

## 服务器信息
- **地址**: http://localhost:8050
- **版本**: 1.0.0
- **Swagger文档**: http://localhost:8050/docs
- **状态**: 运行中

## 真实接口清单

### 1. 健康检查
```
GET /health
```
**响应示例**:
```json
{
  "status": "healthy",
  "service": "xDAN Rag Copilot API Service",
  "version": "1.0.0",
  "timestamp": "2025-06-26T19:32:49.442980"
}
```
- **状态**: ✅ 正常
- **响应时间**: ~30ms

### 2. 知识库管理

#### 2.1 获取知识库列表
```
GET /api/v1/datasets?page=1&page_size=5
```
**响应格式**:
```json
{
  "code": 0,
  "data": [
    {
      "id": "知识库ID",
      "name": "知识库名称",
      "description": "描述",
      "chunk_count": 0,
      "document_count": 0,
      "embedding_model": "BAAI/bge-m3@SILICONFLOW",
      "status": "1",
      "create_date": "创建时间",
      // ... 更多字段
    }
  ]
}
```
- **状态**: ✅ 正常
- **响应时间**: ~400ms

#### 2.2 创建知识库
```
POST /api/v1/datasets
```
**请求体**:
```json
{
  "name": "知识库名称",
  "description": "描述",
  "embedding_model": "BAAI/bge-m3@SILICONFLOW",
  "chunk_method": "naive",
  "parser_config": {
    "chunk_token_num": 512,
    "delimiter": "\n"
  }
}
```
**响应格式**:
```json
{
  "code": 0,
  "data": {
    "id": "新创建的知识库ID",
    "name": "知识库名称",
    // ... 完整知识库信息
  }
}
```
- **状态**: ✅ 正常
- **响应时间**: ~430ms

#### 2.3 更新知识库
```
PUT /api/v1/datasets/{dataset_id}
```
**请求体**:
```json
{
  "name": "新名称",
  "description": "新描述"
}
```
**响应格式**:
```json
{
  "code": 0
}
```
- **状态**: ✅ 正常
- **响应时间**: ~410ms

#### 2.4 删除知识库
```
DELETE /api/v1/datasets/{dataset_id}
```
- **状态**: ✅ 正常
- **响应时间**: ~140ms

### 3. 文档管理

#### 3.1 获取文档列表
```
GET /api/v1/datasets/{dataset_id}/documents?page=1&page_size=5
```
**响应格式**:
```json
{
  "code": 0,
  "data": {
    "docs": [
      {
        "id": "文档ID",
        "name": "文档名称",
        "size": 文件大小,
        "chunk_count": chunk数量,
        "run": "DONE",
        "type": "pdf/doc",
        // ... 更多字段
      }
    ],
    "total": 总数
  }
}
```
- **状态**: ✅ 正常
- **响应时间**: ~400ms

#### 3.2 上传文档
```
POST /api/v1/datasets/{dataset_id}/documents
```
- **Content-Type**: multipart/form-data
- **字段**: file（文件）
- **状态**: 未测试（需要文件上传）

#### 3.3 删除文档
```
DELETE /api/v1/datasets/{dataset_id}/documents/{document_id}
```
- **状态**: 未测试

### 4. 知识库检索
```
POST /api/v1/retrieval
```
**请求体**:
```json
{
  "question": "查询问题",
  "dataset_ids": ["知识库ID"],
  "page": 1,
  "page_size": 3
}
```
**响应格式**:
```json
{
  "code": 0,
  "data": {
    "chunks": [
      {
        "id": "chunk_id",
        "content": "原始内容",
        "content_ltks": "分词后内容",
        "similarity": 0.694,
        "document_id": "文档ID",
        "document_keyword": "文档名称",
        "highlight": "高亮显示内容",
        // ... 更多字段
      }
    ],
    "doc_aggs": [
      {
        "doc_id": "文档ID",
        "doc_name": "文档名称",
        "count": 命中次数
      }
    ],
    "total": 总数
  }
}
```
- **状态**: ✅ 正常
- **响应时间**: ~1.9秒

### 5. 对话管理

#### 5.1 创建对话
```
POST /api/v1/chats
```
**请求体**:
```json
{
  "name": "对话名称",
  "dataset_ids": ["知识库ID"],
  "description": "对话描述"
}
```
**响应格式**:
```json
{
  "code": 0,
  "data": {
    "id": "对话ID (UUID格式)",
    "name": "对话名称",
    "description": "描述",
    "dataset_ids": ["知识库ID"],
    "llm": {
      "model_name": "deepseek-chat",
      "temperature": 0.7,
      "max_tokens": 2000
    },
    "create_date": "创建时间"
  }
}
```
- **状态**: ✅ 正常
- **响应时间**: ~20ms

#### 5.2 获取对话列表
```
GET /api/v1/chats?page=1&page_size=5
```
**响应格式**:
```json
{
  "code": 0,
  "data": [
    {
      "id": "对话ID",
      "name": "对话名称",
      "description": "描述",
      "dataset_ids": ["知识库ID"],
      "llm_config": {
        "model_name": "deepseek-chat",
        "temperature": 0.7,
        "max_tokens": 2000
      },
      "created_at": "创建时间",
      "updated_at": "更新时间"
    }
  ],
  "meta": {
    "page": 1,
    "size": 5,
    "total": 总数,
    "has_next": false,
    "has_prev": false
  }
}
```
- **状态**: ✅ 正常
- **响应时间**: ~6ms

#### 5.3 发送消息（重要）
```
POST /api/v1/chats/{chat_id}/completions
```
**请求体**:
```json
{
  "content": "消息内容",
  "stream": false  // 或 true
}
```

##### 非流式响应（stream: false）
**响应格式**:
```json
{
  "code": 0,
  "data": {
    "answer": "AI的回答内容",
    "reference": {},
    "session_id": "对话ID"
  }
}
```
- **状态**: ✅ 正常
- **响应时间**: ~10秒

##### 流式响应（stream: true）
**响应格式**: Server-Sent Events (SSE)
```
Content-Type: text/event-stream

data: {"code": 0, "message": "", "data": {"answer": "累积的回答内容", "reference": {}, "session_id": "对话ID"}}

data: {"code": 0, "message": "", "data": true}
```
- **状态**: ❌ 错误
- **错误信息**: "所有模型都不可用，最后错误: object async_generator can't be used in 'await' expression"
- **问题**: 流式响应实现有bug

#### 5.4 获取消息历史
```
GET /api/v1/chats/{chat_id}/messages?page=1&page_size=10
```
**响应格式**:
```json
{
  "code": 0,
  "data": [
    {
      "id": "消息ID",
      "role": "user/assistant",
      "content": "消息内容",
      "metadata": {},
      "created_at": "创建时间"
    }
  ],
  "meta": {
    "page": 1,
    "size": 10,
    "total": 总数,
    "has_next": false,
    "has_prev": false
  }
}
```
- **状态**: ✅ 正常
- **响应时间**: ~10ms

#### 5.5 删除对话
```
DELETE /api/v1/chats/{chat_id}
```
**响应格式**:
```json
{
  "code": 0,
  "message": "对话删除成功"
}
```
- **状态**: ✅ 正常
- **响应时间**: ~10ms

## 关键发现

### 1. 请求格式要求
- **认证**: 所有API请求需要 `Authorization: Bearer {token}` 头
- **Content-Type**: JSON请求需要 `Content-Type: application/json`
- **文件上传**: 使用 `multipart/form-data`

### 2. 统一响应格式
所有接口响应都使用统一格式：
```json
{
  "code": 0,        // 0表示成功，非0表示错误
  "data": {},       // 响应数据
  "message": ""     // 错误信息（可选）
}
```

### 3. 流式响应问题
- **现象**: 聊天接口支持流式响应，但需要在请求体中设置 `"stream": true"`
- **默认值**: `stream` 默认为 `false`，返回非流式JSON响应
- **Bug**: 流式响应目前有实现问题，返回500错误

### 4. 性能特点
- **快速接口**: 对话管理接口响应极快（<20ms）
- **中速接口**: 知识库管理接口响应适中（~400ms）
- **慢速接口**: 检索接口（~2秒）和聊天接口（~10秒）响应较慢

### 5. ID格式
- **知识库ID**: 32位十六进制字符串（如：7e8d9e924cde11f0afc90242ac140006）
- **对话ID**: 标准UUID格式（如：818f4bc1-0c26-48dd-b285-4fe680f2d223）

## 建议

1. **修复流式响应**: 当前流式响应实现有bug，需要修复async generator的使用问题
2. **优化响应时间**: 聊天接口响应时间较长，考虑实现真正的流式响应改善用户体验
3. **文档更新**: 更新API文档，明确说明 `stream` 参数的使用方式
4. **错误处理**: 增强错误信息的描述性，帮助开发者快速定位问题

## 测试命令参考

```bash
# 非流式聊天
curl -X POST http://localhost:8050/api/v1/chats/{chat_id}/completions \
  -H "Authorization: Bearer ragflow-g4ZWE3OTNhNDUxYTExZjA4MTljMDI0Mm" \
  -H "Content-Type: application/json" \
  -d '{"content": "你好", "stream": false}'

# 流式聊天（当前有bug）
curl -N -X POST http://localhost:8050/api/v1/chats/{chat_id}/completions \
  -H "Authorization: Bearer ragflow-g4ZWE3OTNhNDUxYTExZjA4MTljMDI0Mm" \
  -H "Content-Type: application/json" \
  -d '{"content": "你好", "stream": true}'
```